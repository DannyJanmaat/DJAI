<!-- Create file: Views/ChatPage.xaml -->
<Page
    x:Class="DJAI.Views.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DJAI.Views"
    xmlns:vm="using:DJAI.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=vm:MainViewModel}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Chat messages -->
        <ScrollViewer Grid.Row="0" Padding="16" x:Name="ChatScrollViewer">
            <ItemsControl ItemsSource="{x:Bind ViewModel.Messages, Mode=OneWay}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,4" 
                                Padding="12" 
                                CornerRadius="8" 
                                Background="{x:Bind IsUser, Converter={StaticResource BoolToBackgroundConverter}}"
                                HorizontalAlignment="{x:Bind IsUser, Converter={StaticResource BoolToAlignmentConverter}}">
                            <StackPanel>
                                <TextBlock Text="{x:Bind IsUser, Converter={StaticResource BoolToSenderConverter}}" 
                                           FontWeight="SemiBold" 
                                           Margin="0,0,0,4"/>
                                <TextBlock Text="{x:Bind Content}" 
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Timestamp}" 
                                           FontSize="12" 
                                           Opacity="0.7" 
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </ScrollViewer>

        <!-- Input area -->
        <Grid Grid.Row="1" Padding="16" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MessageTextBox" 
                     Text="{x:Bind ViewModel.UserInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     PlaceholderText="Type a message..." 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True"
                     MinHeight="40"
                     MaxHeight="120"
                     VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" Margin="8,0,0,0">
                <Button x:Name="SendButton" 
                        Content="Send" 
                        Command="{x:Bind ViewModel.SendMessageCommand}"
                        IsEnabled="{x:Bind ViewModel.IsGenerating, Mode=OneWay, Converter={StaticResource BoolNegationConverter}}"/>

                <Button x:Name="CancelButton" 
                        Content="Cancel" 
                        Command="{x:Bind ViewModel.CancelGenerationCommand}"
                        Visibility="{x:Bind ViewModel.IsGenerating, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>
            </StackPanel>
        </Grid>

        <!-- Status bar -->
        <TextBlock Grid.Row="2" 
                   Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" 
                   Padding="16,4"
                   FontSize="12" 
                   Opacity="0.7"/>
    </Grid>
</Page>
